# Runtime dependencies not otherwise declared (pyproject.toml is tooling-only).
# msgpack is required for the stage-4 memmap dataset format (utils/memmap_dataset.py,
# utils/build_dataset.py). The legacy .pt caches work without it.
msgpack
# wordfreq is used ONLY by the font augmentation (utils/font_synth.py, default
# --font-word-source); a non-font build does not need it. `nltk` or a wordlist
# file are alternative --font-word-source values.
wordfreq
torch
torchvision
diffusers
transformers
tqdm
einops
scikit-image
opencv-python
